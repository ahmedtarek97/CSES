#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,m,k,x,c=0,avl=0;
    vector<int>ns;
    vector<int>as;
    cin>>n>>m>>k;
    for(int i=0;i<n;i++){
        cin>>x;
        ns.push_back(x);
    }
    for(int i=0;i<m;i++){
        cin>>x;
        as.push_back(x);
    }
    sort(ns.begin(),ns.end());
    sort(as.begin(),as.end());
    for(int i=0;i<n;i++){
        
    }
    cout<<c<<endl;

}